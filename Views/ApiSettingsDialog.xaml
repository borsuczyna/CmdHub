<Window x:Class="CmdHub.Views.ApiSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remote Settings"
        Width="500" Height="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A8A8A8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2E2E2E"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#E0E0E0"/>
        </Style>

        <Style x:Key="BtnStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="API PORT" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="TxtApiPort" Style="{StaticResource InputStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="CONTROL PANEL PORT" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="TxtControlPanelPort" Style="{StaticResource InputStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="CONTROL PANEL PASSWORD" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="TxtPassword" Style="{StaticResource InputStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal">
            <Button Content="Generate Random"
                    Style="{StaticResource BtnStyle}"
                    Click="BtnGenerate_Click"/>
            <TextBlock Text="Share this password only with trusted users."
                       VerticalAlignment="Center"
                       Foreground="#8C8C8C"
                       FontSize="11"/>
        </StackPanel>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"
                    Style="{StaticResource BtnStyle}"
                    Margin="0,0,10,0"
                    Click="BtnSave_Click"/>
            <Button Content="Cancel"
                    Padding="12,6"
                    Background="#3A3A3A"
                    Foreground="#E0E0E0"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
